# Copy this file to .env and fill in the values before running the services

# MongoDB connection (use a local MongoDB URI or a managed MongoDB Atlas URI)
MONGO_URI=mongodb://127.0.0.1:27017/lawgpt

# JWT secret used by Node server to validate tokens
JWT_SECRET=your_jwt_secret_here

# Hugging Face token (optional, used if loading HF models or tokenizers)
HF_TOKEN=your_hf_token_here

# Ports
PORT=5000            # Node server port
FLASK_PORT=5001      # Flask server port

# Node -> Flask communication
NODE_SERVER_URL=http://localhost:5000
FLASK_SERVER_URL=http://localhost:5001

# Feature flags
ENABLE_SEMANTIC_SEARCH=true
TOP_N_RELEVANT_MESSAGES=5
RECENCY_WEIGHT=0.3

# Google / Gemini (optional)
# If you are using Google Generative Models (Gemini), set this. Not required if using LAWGPT-3.5.
GOOGLE_API_KEY=
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# External LLM endpoints (configured in backend/app.py -> MODEL_ENDPOINTS)
# Example: LAWGPT-3.5 endpoint used by RAG flow
# MODEL_URL_LAWGPT_3_5=https://your-lawgpt-3-5-endpoint.example/generate_from_ids

# Note:
# - Do NOT commit your real .env file to version control. Keep secrets out of the repo.
# - After filling values, run `set` on Windows or `export` on Unix shells, or use a tool like direnv.
